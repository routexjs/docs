{
  "name": "routex-docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "docusaurus start",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "format": "prettier --write 'src/**/*' docusaurus.config.js sidebars.js package.json 'docs/**/*'"
  },
  "dependencies": {
    "@docusaurus/core": "^2.0.0-alpha.50",
    "@docusaurus/preset-classic": "^2.0.0-alpha.50",
    "classnames": "^2.2.6",
    "react": "^16.14.0",
    "react-dom": "^16.8.4"
  },
  "devDependencies": {
    "prettier": "^2.0.4"
  },
  "browserslist": {
    "production": [
      ">1%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 2 chrome version",
      "last 2 firefox version",
      "last 2 safari version"
    ]
  }
}
